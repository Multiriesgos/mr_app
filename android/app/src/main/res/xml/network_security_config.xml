<?xml version="1.0" encoding="utf-8"?>
<!--
  Certificate pinning — secure.multiriesgos.com
  Estrategia : Intermedio Let's Encrypt R12 (estable ~90 días sin rotación)
  SPKI R12   : kZwN96eHtZftBWrOZUsd6cA4es80n3NzSk/XtYz2EqQ=
  R12 expira : 2027-03-12  →  RENOVAR ANTES DE 2027-02-28
  Verificado : 2026-06-06 vía openssl s_client + SPKI DER SHA-256

  Al renovar (solo si Let's Encrypt cambia su intermedio):
  1. openssl s_client -connect secure.multiriesgos.com:443 -showcerts 2>/dev/null \
       | awk '/BEGIN CERTIFICATE/{c++} c==2{print}' \
       | openssl x509 -pubkey -noout \
       | openssl pkey -pubin -outform der \
       | openssl dgst -sha256 -binary | openssl enc -base64
  2. Actualizar el valor del <pin> abajo
  3. Actualizar _spkiPin en lib/core/network/mr_http_client.dart
  4. Actualizar NSPinnedDomains en ios/Runner/Info.plist
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="false">
        <domain includeSubdomains="false">secure.multiriesgos.com</domain>

        <pin-set expiration="2027-02-28">
            <!-- SPKI SHA-256 del intermedio Let's Encrypt R12 — renovar antes de 2027-02-28 -->
            <pin digest="SHA-256">kZwN96eHtZftBWrOZUsd6cA4es80n3NzSk/XtYz2EqQ=</pin>
        </pin-set>

        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </domain-config>

    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
</network-security-config>
